function getData(){
    //filtrar por id del usuario loggeado (Está en el localstorage)
}

function saveData(){
    //enviar con id del usuario loggeado (Está en el localstorage)
    const user = JSON.parse(localStorage.getItem("user"))

    // peticion post con user.id
}